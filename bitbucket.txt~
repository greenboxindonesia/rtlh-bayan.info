$ mkdir /path/to/folder/project
$ cd /path/to/folder/project
$ git init
$ git remote add origin git@bitbucket.org:anggo/kec.bayan.git

$ echo "anggo .ss" >> contributors.txt
$ git add contributors.txt
$ git commit -m 'Initial commit with contributors'
$ git push -u origin master

OR use it:

$ git add --all
$ git commit -m 'Initial commit with contributors'
$ git push -u origin master
